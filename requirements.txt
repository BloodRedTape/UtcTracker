telethon>=1.36,<2.0
discord.py>=2.3,<3.0
fastapi>=0.104
uvicorn[standard]>=0.24
# Used directly for the daily DB backup upload (also a discord.py dependency)
aiohttp>=3.9
# Security dependencies
cryptography>=41.0.0  # For secure data handling
# Monitoring (GlitchTip / Sentry). enable_logs requires sentry-sdk>=2.20
sentry-sdk>=2.20.0
